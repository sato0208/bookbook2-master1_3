<td><%= comment.comment %></td>
<!-- コメントの削除ボタン -->
<% if user_signed_in? && comment.user_id == current_user.id %>
	<td>
		<%= link_to "comment_Destroy", book_book_comment_path(comment.book_id,comment.id),class:"btn btn-danger", method: :delete, remote: true, data: {confirm: 'コメントを削除しますか？'} %>
	</td>
<% end %>
</tr>